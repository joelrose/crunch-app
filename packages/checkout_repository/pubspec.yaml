name: checkout_repository
description: A Flutter implementation of the TodosApi that uses local storage.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  local_storage_checkout_api: 
    path: ../local_storage_checkout_api

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^0.2.0
