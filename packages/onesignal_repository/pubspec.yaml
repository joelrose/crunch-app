name: onesignal_repository
description: A dart package for the OneSignal Integration

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  onesignal_flutter: ^3.2.8
  authentication_repository: 
    path: ../authentication_repository

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^0.3.0
  test: any