name: hermes_repository
description: A dart package for the Hermes API

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  hermes_api: 
    path: ../hermes_api
  authentication_repository: 
    path: ../authentication_repository